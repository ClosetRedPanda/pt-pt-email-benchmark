__pycache__/
.pytest_cache/
*.pyc
.env
models/*
results/*
*.manifest.json

# Managed model resources (fetched by `python runner.py setup`)
models/*.ftz
models/*.bin

# ...but keep the provenance README for the models directory tracked.
!models/README.md
